<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
    integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous" />
  <title>URL - TinyApp Example</title>
</head>

<body>
  <%- include('partials/_header') %>
    <% if (user && user.id === urlDatabase[id].userID) { %>

      <div class="card text-center">
        <div class="card-header"></div>
        <div class="card-body">
          <h5 class="card-title">TinyURL for: <%= longURL %>
          </h5>
          <p class="card-text">
            Short URL ID: <a href="/u/<%= id %>">
              <%= id %>
            </a>
          </p>
          <h2>Edit</h2>
          <form method="POST" action="/urls/<%= id %>?_method=PUT">
            <label>New URL:</label>
            <input name="newLongURL">
            <button type="submit" class="btn btn-primary">Edit</button>     
          </form>
          <p>This page has been viewed <%= (views || 0) %> times by <%= numofUniques %> unique visitors </p>
          <h3>Visitors to this link</h3>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">User ID</th>
                <th scope="col">Time</th>
              </tr>
            </thead>
            <tbody>
              <% for (let i = 0; i < urlDatabase[id].views.uniques.length; i++) { %>
                <tr>
                  <td><%= urlDatabase[id].views.uniques[i] %></td>
                  <td><%= urlDatabase[id].views.times[i] %></td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
        <div class="card-footer text-muted"></div>
      </div>
      <% } else if (user && user.id !== urlDatabase[id].userID) { %>
        <p>You can only view URLs that you have created</p>
      <% } else { %>
        <p>You must be logged in to view URL information</p>

        <% } %>

          <!-- Bootstrap JS -->
          <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
            integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
            crossorigin="anonymous"></script>
          <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
            integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
            crossorigin="anonymous"></script>
          <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
            integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
            crossorigin="anonymous"></script>
</body>

</html>